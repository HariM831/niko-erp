node_modules/
dist/
.env
uploads/
*.log

# Zoho migration dump — live business data, never committed
.zoho-dump/

# Stamped copies written by scripts/check-capture-stamp.ts for eyeballing.
# Regenerated on every run; the originals beside them are the fixtures.
fixtures/bills/_stamped/

# Amino feed-mill export — real prices and recipes, brought in for the import
# script and never committed. Same rule as .zoho-dump.
feed-mill-export.json
tmp-statements/

# The Amino export: real farm records and photographs of the sheds.
farm-export/
farm-export.tgz
